import React from "react";
import {Container, ProjectDiv, ProjectTitle} from "./StyledComponents";

const Sidebar = () => {
  return (
    <Container>
      <ProjectDiv>
        <ProjectTitle>Projetos</ProjectTitle>
      </ProjectDiv>
    </Container>
  )
};

export default Sidebar;